# Early incomplete eval: full Minerva, n=8. Truncate base/GRPO to the same n
# for first-look three-arm pass@k before a longer ES train + n=64 eval.

[profile]
num_problems = -1
shard_size = 8
num_shards = 1
# n = 8

[eval]
ks = [1, 2, 4, 8]
